export class Drug {
    constructor(id, name, approved, minDose, maxDose, availableAt) {
        this.id = id;
        this.name = name;
        this.approved = approved;
        this.minDose = minDose;
        this.maxDose = maxDose;
        this.availableAt = availableAt;
    }
}
